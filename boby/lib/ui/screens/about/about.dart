import 'package:flutter/material.dart';

class AbautScreen extends StatelessWidget {
  const AbautScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [const Text("About")]);
  }
}
