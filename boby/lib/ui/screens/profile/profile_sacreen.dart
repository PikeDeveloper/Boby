import 'package:flutter/material.dart';

class ProfileSacreen extends StatelessWidget {
  const ProfileSacreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Profile"),
      ),
    );
  }
}